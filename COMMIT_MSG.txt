design: overhaul selection system — remove all border-white/10, adopt premium inner-glow states

- DESIGN.md: raise VARIANCE to 8, codify Border & Selection Policy (inner glow, background tint, scale — NO white borders)
- index.css: full rewrite of component classes (nav, version cards, pipeline nodes, annotation canvas, status pills, sliders)
  - Nav: ::after pseudo overlay for hover, inner-glow nav-active-surface for selected state
  - Version cards: box-shadow inset 0 0 0 1px for structural border; inner-glow + tinted bg for selected
  - Pipeline node cards: same structural/selected pattern
  - Icon buttons: inner glow instead of border for active state
  - All dividers downgraded to border-white/[0.06] (barely visible, structural only)
  - Upload/drop zone: removed border, kept inner-glow for active indicator
  - Metric lines: removed border, use bg-white/[0.03] for depth
  - Asset select control: kept structural border for checkbox affordance
  - Geometry input: focus ring instead of focus border
  - Annotation canvas: structural border remains for canvas boundary
- App.tsx: remove border-white/10 from all className strings (38 occurrences), drop explicit borders from version-card, pipeline-node-card, IconButton, MetricLine, upload button, nav-logo
- AnnotationEngine.tsx: same border cleanup, update IconButton className, GeometryInput focus style
- typecheck passes
