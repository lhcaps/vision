# Vision Cursor Agent Baseline (GEMINI.md placeholder)

# This mirrors Gemini's conventions for cross-tool compatibility

- Read AGENTS.md first before editing any file.
- Inspect validation and evaluation feedback under .metaharness-vision/runs before proposing changes.
- The Vision monorepo uses pnpm workspaces. Always run commands from the repo root.
- TypeScript strict mode is enforced. All code must pass `pnpm typecheck`.
